# Feedback

## Overlay setting

I want to introduce a setting which display the overlay in a different style instead of 1 column. I want to have multiple columns (per setting slider) and additionally visualisation to have it grouped by melee, ranged & healer.

Introduce those two settings and build the different overlay visualisations

## Completion checkmark

We got that wrong. I meant that the checkmark should replace the rating being shown. so the checkmark needs to replace the rating text and be centered correctly.

## Rating text

Make it have more room on some scales it truncated some ratings - a few more pixels doesn't really hurt

## Glow to logged in character

If I am logged in into a spec / class which is part of the active challenge. add some kind of glow / indicator on which I am logged in.

## Export everything

We have heavily focused on character export/import. I think we should export/import everything, challenges, characters and settings.